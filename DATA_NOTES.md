# Data Notes

## English

Short notes:
- `PART B` starts on page `89`.
- `B.1` starts on page `89`.
- `B.2` starts on page `123`.
- For now, the target range is pages `89` to `122`.

Useful clues:
- Later pages repeat headers like `Part B.1.` or `Part B.1`.
- INID labels such as `111`, `151`, `210`, and `400` appear as separate text blocks.
- The layout is clearly split into two columns.
- Some records may continue into the next column or page.

Working idea:
filter pages, sort blocks by reading order, group by record start, then map fields.

## Español

Notas cortas:
- `PART B` empieza en la página `89`.
- `B.1` empieza en la página `89`.
- `B.2` empieza en la página `123`.
- Por ahora, el rango objetivo es de la página `89` a la `122`.

Pistas útiles:
- En las páginas siguientes se repiten encabezados como `Part B.1.` o `Part B.1`.
- Los INID `111`, `151`, `210` y `400` aparecen como bloques separados.
- El layout está claramente dividido en dos columnas.
- Algunos registros pueden seguir en otra columna o página.

Idea de trabajo:
filtrar páginas, ordenar bloques, agrupar por inicio de registro y luego mapear campos.
