[project]
name = "unumbio-pdf-processing"
version = "0.1.0"
description = "Incremental solution for the UNUMBIO PDF processing test."
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
